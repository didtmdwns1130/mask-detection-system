# Mask Detection System 😷

마스크 착용 여부를 실시간으로 감지하는 시스템입니다.  
Python FastAPI 서버와 MFC C++ 클라이언트로 구성되어 있으며,  
Basler 산업용 카메라 또는 웹캠을 통해 사람 얼굴을 인식한 후  
마스크 착용 상태를 분석 및 표시합니다.

---

## 주요 기능

- 얼굴 인식 및 마스크 착용 여부 판별  
- 실시간 영상 스트리밍 처리  
- Python FastAPI 기반 REST API 서버  
- MFC C++ 클라이언트 UI  
- Basler 산업용 카메라(pypylon) 또는 웹캠 지원  
- 마스크 착용 상태(착용/미착용)에 따라  
  색상 박스와 확률(%)을 오버레이하여 표시  

---

## 아키텍처 구조

- **Server (Python FastAPI)**  
  - 이미지 프레임 수신  
  - 얼굴 검출 및 마스크 착용 판단  
  - 처리 결과를 JSON 형태로 반환  

- **Client (MFC C++ App)**  
  - 서버에서 처리된 결과를 수신  
  - GDI+ 기반 실시간 이미지 렌더링  
  - 착용/미착용에 따라 Bounding Box 색상 변경  

---

## 사용 기술

- **Python 3 (FastAPI, DeepFace, OpenCV, pypylon)**  
- **C++ (MFC, WinHTTP, GDI+)**  
- **Basler Industrial Camera**  
- **JSON 기반 서버–클라이언트 통신**  

---

## 파일 설명

- **mask_server/** : FastAPI 기반 Python 서버 코드  
- **mask_client/** : MFC C++ 클라이언트 코드  
- **model/** : 마스크 감지 모델 및 관련 파일  

---

## 실행 환경 (Environment)

본 시스템은 아래 환경에서 개발 및 테스트되었습니다.

### 서버(Python FastAPI)
- 운영체제: **Windows 10**
- Python 3.10 이상  
- 라이브러리: FastAPI, uvicorn, OpenCV, DeepFace, pypylon  
- 카메라: Basler 산업용 카메라

### 클라이언트(MFC C++ App)
- 운영체제: **Windows 10**
- IDE: Visual Studio 2022  
- 기술 스택: MFC, WinHTTP, GDI+  

**주의:**  
- MFC 클라이언트는 Windows 환경에서만 실행 가능합니다.  
- Basler 카메라 사용 시 pypylon 설치가 필요합니다.  
- 산업용 카메라 환경에서는 반드시 USB3 / GigE 드라이버가 올바르게 설치되어야 합니다.

---

## 실행 방법

### 1. Python 서버 실행

`app.py` 파일 내부에서 `uvicorn.run()`으로 서버가 자동 실행되도록 구성되어 있습니다.

따라서 VS Code에서 `app.py`를 연 뒤, 상단의 **실행 버튼(▶)** 을 클릭하면 서버가 시작됩니다.

- 서버 IP : `127.0.0.1`
- 포트 번호 : `8000


### 2. MFC 클라이언트 실행

- 서버 주소를 `127.0.0.1` 로 설정합니다.
- 포트 번호는 `8000` 으로 설정합니다.
- Start 버튼을 클릭하면 실시간 감지가 시작됩니다.





